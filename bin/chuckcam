#!/bin/bash

#tmux new-session -d -s chuckcam
#tmux send-keys -t chuckcam "ssh -CY scexao-op@localhost \"source scexao-set-env.sh; PATH=\\\${HOME}/anaconda3/bin:\${PATH} chuckcam.py\"" Enter

chuckcam.py $@
